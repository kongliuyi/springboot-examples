server:
  port: 8080



spring:
  application:
    name: redis-config
  redis:
    host: ${REDIS_HOST:192.168.36.130}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:123456}
    database: 0
    lettuce:
      pool:
        max-active: 32
        max-wait: 300
        max-idle: 16
        min-idle: 8






